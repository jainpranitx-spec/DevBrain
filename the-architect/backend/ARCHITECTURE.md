# DevBrain Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                     USER BROWSER                                      │
├─────────────────────────────────────────────────────────────────────┤
│  http://localhost:5173                                                │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    React Frontend                             │   │
│  │  (The Architect)                                              │   │
│  │                                                                │   │
│  │  ┌─────────────┐    ┌──────────────┐    ┌──────────────┐    │   │
│  │  │  Navbar     │    │  TreeView    │    │  GraphView   │    │   │
│  │  │             │    │              │    │              │    │   │
│  │  │ Toggle View │    │ Hierarchical │    │ Force-Directed
│  │  └─────────────┘    └──────────────┘    └──────────────┘    │   │
│  │         ↓                                                      │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │             ChatPanel (Context-Aware)                │   │   │
│  │  │                                                        │   │   │
│  │  │  💬 "Break this down into tasks..."                  │   │   │
│  │  │     ↓                                                 │   │   │
│  │  │  📚 Using knowledge: research.pdf, notes.md          │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  │         ↓                                                      │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │        Zustand Store (Global State)                  │   │   │
│  │  │  - currentView, nodes, edges                         │   │   │
│  │  │  - selectedNodeId, chatHistory                       │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  │         ↓                                                      │   │
│  │  ┌──────────────────────────────────────────────────────┐   │   │
│  │  │     services/api.js (HTTP Client)                    │   │   │
│  │  │  createNode(), chatWithAI(),                         │   │   │
│  │  │  uploadKnowledge(), updateNodeStatus()              │   │   │
│  │  └──────────────────────────────────────────────────────┘   │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                           ↓                                           │
│                  VITE_API_URL=localhost:8000/api                     │
│                                                                        │
└─────────────────────────────────────────────────────────────────────┘
                              ↓ (HTTP/JSON)
                         CORS Allowed
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│                    DJANGO BACKEND SERVER                              │
├─────────────────────────────────────────────────────────────────────┤
│  http://localhost:8000                                                │
│                                                                        │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                   URL Router                                   │  │
│  │  config/urls.py                                               │  │
│  │  ├─ /api/projects/        → ProjectViewSet                   │  │
│  │  ├─ /api/nodes/           → NodeViewSet                      │  │
│  │  ├─ /api/edges/           → EdgeViewSet                      │  │
│  │  ├─ /api/knowledge/       → KnowledgeBaseViewSet            │  │
│  │  ├─ /api/chat/node/{id}/  → ChatNodeView                    │  │
│  │  └─ /api/search/knowledge/→ SearchKnowledgeView             │  │
│  └───────────────────────────────────────────────────────────────┘  │
│           ↓                                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │        REST API Views (api/views.py)                          │  │
│  │                                                                │  │
│  │  ┌─────────────┐  ┌──────────┐  ┌──────────┐                │  │
│  │  │ ProjectView │  │ NodeView │  │ChatView  │                │  │
│  │  │             │  │          │  │          │                │  │
│  │  │ CRUD Ops    │  │ CRUD Ops │  │ POST msg │                │  │
│  │  │ + Export    │  │ + Search │  │ + Search │                │  │
│  │  └─────────────┘  └──────────┘  └──────────┘                │  │
│  │                                                                │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │        KnowledgeBaseView                             │   │  │
│  │  │  - Upload files (PDF, DOCX, TXT, MD)               │   │  │
│  │  │  - Extract text content                            │   │  │
│  │  │  - Search/index for retrieval                      │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────────────┘  │
│           ↓                                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │        Business Logic (api/services.py)                       │  │
│  │                                                                │  │
│  │  ┌──────────────────────────┐  ┌──────────────────────────┐  │  │
│  │  │  KnowledgeSearchService  │  │ GeminiAIService          │  │  │
│  │  │                          │  │                          │  │  │
│  │  │ 1. Keyword matching      │  │ 1. Build context        │  │  │
│  │  │ 2. Score results         │  │ 2. Call Gemini API      │  │  │
│  │  │ 3. Return top K          │  │ 3. Fallback to mock     │  │  │
│  │  │                          │  │ 4. Save messages        │  │  │
│  │  │ Used for:                │  │                          │  │  │
│  │  │ - AI context             │  │ Fallback when:           │  │  │
│  │  │ - Chat grounding         │  │ - No API key            │  │  │
│  │  │                          │  │ - Network error         │  │  │
│  │  │                          │  │ - API unavailable       │  │  │
│  │  └──────────────────────────┘  └──────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│           ↓                                                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │           Database Layer (SQLite / PostgreSQL)                │  │
│  │                                                                │  │
│  │  ┌──────────────────────────────────────────────────────┐   │  │
│  │  │                    Project                            │   │  │
│  │  │  id | name | owner | created_at | updated_at        │   │  │
│  │  └──────────────────────────────────────────────────────┘   │  │
│  │                           ↓ (1:N)                            │  │
│  │  ┌─────────────────────────────┬──────────────────────────┐ │  │
│  │  │                             │                          │ │  │
│  │  ↓                             ↓                          ↓ │  │
│  │  │                             │                          │ │  │
│  │  ┌──────────────┐  ┌──────────────────┐  ┌─────────────┐│  │  │
│  │  │    Node      │  │   KnowledgeBase  │  │   ChatMsg   ││  │  │
│  │  │              │  │                  │  │             ││  │  │
│  │  │ id           │  │ id               │  │ id          ││  │  │
│  │  │ label        │  │ title            │  │ node_id (FK)││  │  │
│  │  │ status       │  │ file_type        │  │ role        ││  │  │
│  │  │ parent (FK)  │  │ full_text        │  │ message     ││  │  │
│  │  │ position_x/y │  │ content_preview  │  │ source      ││  │  │
│  │  │ owner        │  │ uploaded_by      │  │ created_at  ││  │  │
│  │  │              │  │                  │  │             ││  │  │
│  │  └──────────────┘  └──────────────────┘  └─────────────┘│  │  │
│  │  (Hierarchical:      (Search Index for    (Per-node   │  │  │
│  │   Parent refs to     Knowledge Grounding)  Conversation)  │  │
│  │   Node.self)                                             │  │  │
│  │         ↓                                                 │  │  │
│  │  ┌──────────────────────────────────────────────────────┐ │  │
│  │  │              Edge (Relations)                         │ │  │
│  │  │  id | source_id | target_id | project_id           │ │  │
│  │  │                                                       │ │  │
│  │  │  (Auto-generated from Node.parent relationships)    │ │  │
│  │  └──────────────────────────────────────────────────────┘ │  │
│  │                                                            │  │
│  └────────────────────────────────────────────────────────────┘  │
│           ↓                                                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │         External Services                                │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │  Google Generative AI (Gemini)                    │  │  │
│  │  │  https://api.generative.ai                        │  │  │
│  │  │                                                    │  │  │
│  │  │  POST {message + context + knowledge}            │  │  │
│  │  │  ← Grounded response based on uploaded files      │  │  │
│  │  │                                                    │  │  │
│  │  │  Config:                                          │  │  │
│  │  │  GEMINI_API_KEY = 'INSERT API KEY'              │  │  │
│  │  │  GEMINI_MODEL = 'gemini-1.5-flash'              │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                                                            │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │        Django Admin Panel                                 │  │
│  │        http://localhost:8000/admin                        │  │
│  │                                                            │  │
│  │  Manage: Projects, Nodes, Edges, KnowledgeBase          │  │
│  │  Edit, delete, view relationships                        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                    │
└─────────────────────────────────────────────────────────────────────┘
```

## Chat Flow (Detailed)

```
User selects "Login System" node and asks:
"How should I architect the authentication?"
        ↓
┌─────────────────────────────────────┐
│  POST /api/chat/node/{node_id}/     │
│  { message, use_knowledge }         │
└─────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────┐
│  ChatNodeView (views.py)                            │
│  1. Save user message to DB                         │
│  2. Create GeminiAIService instance                 │
│  3. Call generate_response()                        │
└─────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────────────┐
│  GeminiAIService.generate_response()                     │
│                                                           │
│  1. Call KnowledgeSearchService.search_relevant...()    │
│     ├─ Search KnowledgeBase full_text                   │
│     ├─ Score by keyword matches                         │
│     ├─ Return top 3 results                             │
│     └─ Example: ["auth-patterns.pdf", "security.md"]   │
│                                                           │
│  2. Build system prompt:                                │
│     ├─ Node context: "Login System, in-progress"       │
│     ├─ Knowledge excerpts from PDFs                     │
│     └─ Instructions: "Answer based on provided info"   │
│                                                           │
│  3. API call decision:                                  │
│     if GEMINI_API_KEY available AND valid:            │
│         → genai.GenerativeModel.generate_content()     │
│     else:                                               │
│         → Mock response (still helpful!)               │
│                                                           │
│  4. Prepare response:                                   │
│     {                                                   │
│       message: "Based on your docs, JWT tokens...",   │
│       source: "gemini-api" or "mock",                 │
│       knowledge_used: true,                            │
│       knowledge_sources: ["auth-patterns.pdf"]         │
│     }                                                   │
└──────────────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────┐
│  Save ChatMessage to database                    │
│  - User message                                   │
│  - AI response                                    │
│  - Linked to Node                                │
└──────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────┐
│  Return JSON Response with:                      │
│  - Both messages                                  │
│  - Metadata (source, knowledge used, sources)   │
└──────────────────────────────────────────────────┘
        ↓
Front-end ChatPanel displays:
  User: "How should I architecture auth?"
  AI: "Based on your documents..."
  📚 Sources: auth-patterns.pdf
```

## Knowledge Indexing Flow

```
User uploads "architecture.pdf"
        ↓
┌──────────────────────────────────────────┐
│  POST /api/knowledge/                    │
│  FormData { file, title, project }       │
└──────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────┐
│  KnowledgeBaseViewSet.perform_create()           │
│                                                   │
│  1. Save file to disk: media/knowledge_files/   │
│  2. Extract text content:                        │
│     ├─ PDF → PyPDF2 extracts text              │
│     ├─ DOCX → python-docx extracts paragraphs  │
│     ├─ TXT/MD → Read as-is                     │
│     └─ Result: full_text field                  │
│  3. Save preview: first 500 characters          │
│  4. Store in KnowledgeBase model                │
└──────────────────────────────────────────────────┘
        ↓
┌──────────────────────────────────────────────────┐
│  Database: KnowledgeBase                         │
│  ├─ file: (PDF file in media/)                  │
│  ├─ full_text: All extracted text              │
│  ├─ content_preview: First 500 chars           │
│  └─ project: Link to Project                    │
└──────────────────────────────────────────────────┘
        ↓
Future chat queries now search:
  KnowledgeBase.objects.filter(project=X)
  → keyword matching in full_text
  → return top matches
  → include in Gemini prompt
```

## State Management

```
Frontend (Zustand Store)
├─ currentView: 'tree' | 'graph'
├─ nodes: Array<Node>
│  └─ {id, label, status, position, owner, parentId}
├─ edges: Array<Edge>
│  └─ {id, source, target}
├─ selectedNodeId: string | null
├─ chatHistory: Object<nodeId, Array<Message>>
│  └─ {role: 'user'|'ai', message, source}
│
└─ Actions:
   ├─ setView(view) → POST /api/nodes/{id}/update_status
   ├─ addNode(data) → POST /api/nodes/ + POST /api/edges/
   ├─ deleteNode(id) → DELETE /api/nodes/{id}
   ├─ selectNode(id) → Local state
   ├─ addChatMessage(nodeId, msg) → POST /api/chat/node/{id}/
   └─ updateNodePosition(id, pos) → POST /api/nodes/{id}/move/

↓ (Sync to Backend)

Backend (Django Models)
├─ Project
├─ Node (with parent FK for hierarchy)
├─ Edge (source → target)
├─ KnowledgeBase
└─ ChatMessage (per-node)
```

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────────────────────┐
│                   Production                                 │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  CDN / Static Files                                  │   │
│  │  (Vercel, Netlify, Cloudflare)                      │   │
│  │  └─ React frontend compiled & cached                 │   │
│  └──────────────────────────────────────────────────────┘   │
│                         ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Application Server                                  │   │
│  │  (Docker container)                                  │   │
│  │  └─ Gunicorn/Django on port 8000                    │   │
│  │  └─ PostgreSQL database                             │   │
│  │  └─ Redis (caching, sessions)                       │   │
│  │  └─ File storage (S3 for knowledge files)           │   │
│  └──────────────────────────────────────────────────────┘   │
│                         ↓                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  External APIs                                       │   │
│  │  └─ Google Generative AI (Gemini)                   │   │
│  │  └─ Email service (SendGrid)                        │   │
│  │  └─ Analytics (Posthog)                             │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

**This diagram shows the complete flow: Frontend UI → REST API → Business Logic → Database & AI Services → User**
